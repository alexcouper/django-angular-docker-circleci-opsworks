machine:
  environment:
    USE_NOSETESTS: 1
  timezone:
    Europe/London
  python:
    version: 3.4.0

dependencies:
  pre:
    - pip3 install -r review/requirements/base.txt -r review/requirements/test.txt

test:
  override:
    - cd review; python3 manage.py test
